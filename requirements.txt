# TDA Pipeline Benchmark — pinned dependencies
# Generated 2026-08-24 for Python 3.12.3
#
# These exact versions are LOAD-BEARING, not hygiene: the weak-alpha
# IndexError fragility, the Sparse Rips 30x-slower-at-design-point result,
# and the 3-27% speed gaps are all giotto-tda 0.6.2 findings (see
# REPRODUCING.md). A newer giotto-tda will not reproduce them.

python==3.12.3
giotto-tda==0.6.2
ripser==0.6.15
gudhi==3.13.0
scikit-learn==1.3.2
numpy==1.26.4
scipy==1.17.1
matplotlib==3.11.1
pyyaml==6.0.3
pandas==3.0.5
statsmodels==0.14.6
